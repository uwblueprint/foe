<p id="notice"><%= notice %></p>

<h1>Sightings</h1>

<table>
  <thead>
    <tr>
      <th>Image</th>
      <th>Date</th>
      <th>Weather</th>
      <th>Habitat</th>
      <th>Species</th>
      <th>Street Address</th>
      <th>Latitude</th>
      <th>Longitude</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sightings.each do |sighting| %>
      <tr>
        <td><%= image_tag sighting.image %></td>
        <td><%= sighting.date %></td>
        <td><%= sighting.weather %></td>
        <td><%= sighting.habitat %></td>
        <td><%= sighting.species %></td>
        <td><%= sighting.street_address %></td>
        <td><%= sighting.latitude %></td>
        <td><%= sighting.longitude %></td>
        <td><%= link_to 'Show', [:admin, sighting] %></td>
        <!-- <td><%= link_to 'Edit', edit_sighting_path(sighting) %></td>
        <td><%= link_to 'Destroy', sighting, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>
